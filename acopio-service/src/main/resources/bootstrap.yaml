spring:
  application:
    name: acopio-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:postgresql://localhost:5432/acopiosDB
    username: postgres
    password: postgres123
  jpa:
    hibernate:
      ddl-auto: update
  management:
    endpoints:
      web:
        exposure:
          include: "*"
